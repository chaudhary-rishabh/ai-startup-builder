{
  "name": "@repo/types",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsup src/index.ts --dts-only --out-dir dist --tsconfig tsconfig.json",
    "clean": "rimraf dist .turbo"
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "tsup": "^8.0.0",
    "typescript": "^6.0.3"
  }
}
